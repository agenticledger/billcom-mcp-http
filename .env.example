# No server-side credentials needed.
# Clients pass their Bill.com credentials as a pipe-delimited Bearer token:
#   Authorization: Bearer devKey|username|password|orgId|environment
#
# environment is optional (defaults to "sandbox")
PORT=3100
SERVER_BASE_URL=https://billcommcp.agenticledger.ai
